language: generic

cache:
  directories:
    - ${HOME}/.gem

before_install:
  - gem update --system
  - gem install bundler

install:
  - bundle install

jobs:
  include:
    - stage: Lint
      script:
        - bundle exec danger

    - stage: Build
      script:
        - bundle exec jekyll build
